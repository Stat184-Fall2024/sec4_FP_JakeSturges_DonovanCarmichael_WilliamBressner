### Making Tables
## Case = NFL Team per year
# Team: x axis
# Year: y axis
# Win percentage: In the table entries

library(ggplot2)
library(dplyr)
library(tidyverse)
library(rvest)
data("")
ggplot( , aes(x = , y = , color = )) +
  geom_point() +
  labs(title = "", 
       x = "", 
       y = "") +
  theme_minimal()
  

